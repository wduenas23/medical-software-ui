export interface FormOfPayment {
    id:          number;
    description: string;
}

export interface MedicalServices {
    id:          number;
    description: string;
    cost:        number;
    category:    number;
}
